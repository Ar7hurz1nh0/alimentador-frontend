# Hello World
> This is a test

```ts
function fat(x: number): number {
  if (x < 0) return NaN;
  if (x === 0) return 1;
  let a: number = x;
  while (--x)
   a *= x;
  return a;
}
```

**Foo** *bar* _toz_